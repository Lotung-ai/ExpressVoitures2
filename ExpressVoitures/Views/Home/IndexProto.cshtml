@using Microsoft.AspNetCore.Mvc.Localization
@inject IViewLocalizer Localizer
@model IEnumerable<ExpressVoitures.Models.Entities.Car>

<!DOCTYPE html>
<html>

<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no" />
    <link href="https://fonts.googleapis.com/css?family=Libre+Franklin|Newsreader|Libre+Baskerville|Inter&display=swap" rel="stylesheet" />
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet" />
    <link rel="stylesheet" href="~/css/styles.css" />
    <title>Express Voitures</title>
</head>

<body>
    <!-- Hero Section -->
    <section class="hero-image">
        <div class="hero-text">
            <h1 class="display-4">Bienvenue chez Express Voitures</h1>
            <a class="btn btn-outline-light" href="#">Voir la liste des voitures</a>
        </div>
    </section>

    <section class="container mt-5">
 
        <h2 class="mb-4">Nouveautés</h2>
 
        <div class="row">
            @{
                @if (Model != null && Model.Any())
                {
                var limitedNouveautes = 0;
                foreach (var item in Model)
                {
                    if (limitedNouveautes >= 2)
                    {
                        break;
                    }
                    <div class="col-md-4 mb-4">
                        <div class="card">
                            <div class="card-body">
                                <h5 class="card-title">@item.Brand?.Name @item.Modele?.Name</h5>
                                <p class="card-text">Date d'achat: @Html.DisplayFor(modelItem => item.DateOfPurchase)</p>
                                <p class="card-text">Prix d'achat: @Html.DisplayFor(modelItem => item.PurchasePrice) euros</p>
                                <p class="card-text">Réparation: @Html.DisplayFor(modelItem => item.Repair)</p>
                                <p class="card-text">Coût de réparation: @Html.DisplayFor(modelItem => item.RepairPrice) euros</p>
                                <p class="card-text">Date de disponibilité: @Html.DisplayFor(modelItem => item.DateOfAvailabilityForSale)</p>
                                <p class="card-text">Prix de vente: @Html.DisplayFor(modelItem => item.SellingPrice) euros</p>
                                <p class="card-text">Date de vente: @Html.DisplayFor(modelItem => item.DateOfSale)</p>
                                <p class="card-text">Année: @Html.DisplayFor(modelItem => item.Year.Value)</p>
                                <p class="card-text">Finition: @Html.DisplayFor(modelItem => item.Finition.Name )</p>
                            </div>
                        </div>
                    </div>
                    limitedNouveautes++;
                }
                }
            
            else
            {
            <p>Aucune voiture n'est disponible pour le moment.</p>
            }
            }
        </div>
    </section>

    <!-- Information Section -->
    <section class="container mt-5">
        <h2>Ce qu’il faut savoir</h2>
        <p>Nos voitures sont achetées dans des ventes aux enchères par des professionnels agréés.</p>
    </section>

    <!-- Footer -->
    <footer class="bg-dark text-white py-3 mt-5">
        <div class="container text-center">
            <p class="mb-0">© 2024 Express Voitures</p>
        </div>
    </footer>

    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.bundle.min.js"></script>
</body>

</html>
